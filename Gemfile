# A sample Gemfile
source "https://rubygems.org"

# gem "rails"

gem 'rspec'
    gem 'pry'

#stars_with_a_vowel?
def stars_with_a_vowel(words)
    if words.match (/^[aeiouAEIOU]/)
        returns false
    else
        returns true
    end
end
